---
layout: post
title: "线程池没有银弹"
date: 2026-3-10
categories: [后端开发]
tags: [java, 线程池, 并发编程, 虚拟线程]
comments: true
author: huanghuoguoguo
---

你用过几种线程池？JDK 的 `ThreadPoolExecutor`、`ForkJoinPool`、Tomcat 的线程池……再加上阿里规约里那句"不准用 `Executors` 创建线程池"，光是 Java 生态里线程池的选择就够纠结的。

问题来了：JDK 已经提供了 `ThreadPoolExecutor`，七个参数什么都能配，为什么还有人造轮子？它们到底在改什么？

再往深想一层：以前总说"线程创建代价大，所以要池化"——到底大在哪？如果虚拟线程创建只要几微秒、几 KB 内存，池化还有必要吗？

这些问题串在一起，最后都会落到一件事上：**池化到底在池什么，什么时候该池，什么时候不该池。**

<!-- more -->

---

## JDK ThreadPoolExecutor：好用，但有脾气

先回顾 JDK `ThreadPoolExecutor` 的核心行为。任务提交时的决策链路：

```
task submitted
  → workerCount < corePoolSize?     → 创建新线程执行
  → workQueue.offer(task) 成功?     → 入队等待
  → workerCount < maximumPoolSize?  → 创建新线程执行
  → 都不行?                         → 执行拒绝策略
```

注意第二步和第三步的顺序：**先入队，再扩线程**。只有队列满了，才会创建超过 corePoolSize 的线程。

这个设计在吞吐量优先的场景下是合理的——尽量复用已有线程，减少线程创建和销毁的开销。但在延迟敏感的场景下，问题就来了：

假设 corePoolSize=10，队列容量 1000。突然来了 100 个请求，10 个核心线程全忙。剩下 90 个任务全部进队列排队，即使你把 maximumPoolSize 设成 200 也没用——队列还没满，不会触发创建新线程。用户看到的就是：服务器明明还有资源，请求却在排队。

### Executors 的四个陷阱

`Executors` 工厂方法是教科书和面试的常客，也是生产环境的常见事故来源：

| 工厂方法 | 队列类型 | 问题 |
|---------|---------|------|
| `newFixedThreadPool(n)` | `LinkedBlockingQueue`（无界） | 任务堆积导致 OOM |
| `newSingleThreadExecutor()` | `LinkedBlockingQueue`（无界） | 同上 |
| `newCachedThreadPool()` | `SynchronousQueue` | maximumPoolSize = Integer.MAX_VALUE，线程数爆炸 |
| `newScheduledThreadPool(n)` | `DelayedWorkQueue`（无界） | 任务堆积导致 OOM |

阿里 Java 规约直接禁止用 `Executors` 创建线程池，要求你手写 `ThreadPoolExecutor`，把有界队列和拒绝策略都摆明白。原因也不复杂：这些默认配置放到生产环境里太容易翻车。

这不是 `Executors` 设计得差——是默认配置面向的场景和生产环境的需求不匹配。

---

## 线程池之间，到底差在哪

既然 `ThreadPoolExecutor` 能配七个参数，理论上什么场景都能覆盖，为什么还有人造轮子？因为**参数能配，不等于行为真能改到位**。不同线程池真正拉开差距的地方，主要还是两块：**提交策略**和**调度模型**。

### 维度一：提交策略——先入队还是先扩线程

这是最常见的改造点。`ThreadPoolExecutor` 的默认策略是"先入队、再扩线程"，这在延迟敏感的场景下很痛。

Tomcat 的 `org.apache.tomcat.util.threads.ThreadPoolExecutor` 直接翻转了这个顺序。它自定义了一个 `TaskQueue`，在 `offer()` 方法里加了判断：如果当前线程数还没到 maximumPoolSize，就返回 false——骗 `ThreadPoolExecutor` 以为队列满了，触发创建新线程。

```
JDK 默认策略：
task → core 线程满了? → 入队 → 队列满了? → 扩线程 → 拒绝

Tomcat 策略：
task → core 线程满了? → 还能扩线程? → 扩线程 → 不能了? → 入队 → 拒绝
```

对 Web 服务器来说这是正确的选择。HTTP 请求是延迟敏感的——用户不会因为"服务器的线程池队列还没满"就愿意多等 500ms。Dubbo 的 `EagerThreadPool` 做的事情本质相同。

这个维度的核心问题是：**你的场景更怕延迟，还是更怕资源浪费？** 怕延迟就先扩线程，怕资源浪费就先入队。

### 维度二：调度模型——共享队列还是工作窃取

`ThreadPoolExecutor` 所有线程从一个共享队列里取任务。`ForkJoinPool` 完全不同：

| 维度 | ThreadPoolExecutor | ForkJoinPool |
|------|-------------------|-------------|
| 任务模型 | 独立任务 | 可分解的递归任务 |
| 调度策略 | 共享队列 | 每个线程有自己的双端队列 + 工作窃取 |
| 适用场景 | 大量独立的 I/O 或计算任务 | 分治（归并排序、并行流） |
| 任务粒度 | 粗（一个 HTTP 请求） | 细（递归拆分到阈值） |

工作窃取（work-stealing）：每个线程从自己队列的头部取任务，空闲时从其他线程队列的**尾部**偷任务。头尾分离减少了线程间的竞争。

Java 8 的 `parallelStream()` 底层就是 `ForkJoinPool.commonPool()`。但要注意：commonPool 是全局共享的，如果你在 parallelStream 里做 I/O 操作阻塞了，会拖慢整个应用的所有并行流。

大多数业务场景处理的是独立任务，用 `ThreadPoolExecutor` 就够了。`ForkJoinPool` 的价值在可以递归分解的计算密集型任务上。

### 不是所有"线程池"都在改池

顺带提一句：像 Spring 的 `ThreadPoolTaskExecutor`，里面还是 `ThreadPoolExecutor`，只是多了一层 Bean 生命周期管理和上下文传递（比如 MDC traceId）；Guava 的 `MoreExecutors` 也是类似，改的是返回值类型（`ListenableFuture`）和线程命名。它们主要解决的是**框架集成**，不是线程池调度行为本身。把“真改了池的行为”和“外面再包一层壳”分开看，选型时就没那么容易被名字绕晕。

---

## 选哪个？看场景

| 场景 | 关键需求 | 选择 |
|------|---------|------|
| Web 服务（延迟敏感） | 快速响应，先扩线程 | Tomcat 线程池 / EagerThreadPool |
| 批量处理（吞吐优先） | 充分利用 CPU，减少切换 | ThreadPoolExecutor（core=CPU 核数） |
| 分治计算 | 递归分解 + 工作窃取 | ForkJoinPool |

如果非要给一个"默认推荐"：

- CPU 密集型：线程数 = CPU 核数（或 +1），用有界队列
- I/O 密集型：线程数 = CPU 核数 × (1 + I/O 等待时间 / CPU 计算时间)，这是 Little's Law 的应用
- 不确定：从 CPU 核数 × 2 开始，压测调整

但这些公式只是起点。真正有效的方式是**压测**——别猜，量。

---

## "创建线程代价大"——到底大在哪？

这句话从学 Java 第一天就听过，可真把“大”拆开讲的人不多。

在 Linux 上创建一个线程，底层是 `clone()` 系统调用。成本拆解：

| 成本项 | 量级 | 说明 |
|--------|------|------|
| 系统调用开销 | ~1-10μs | 用户态 → 内核态切换，分配 task_struct |
| 内核栈 | 8-16KB | 每个线程的内核栈 |
| 用户态栈 | 512KB-1MB | JVM 默认 `-Xss` 通常 512KB，Linux 默认 `ulimit -s` 8MB |
| 线程本地存储 | 几 KB | TLS 初始化 |
| **总计** | **~10-100μs + ~0.5-1MB 内存** | |

单看一个线程，10 微秒不算什么。但如果每个请求创建一个线程，QPS 1 万，每秒就要创建 1 万个线程再销毁 1 万个。光创建销毁就吃掉 100ms-1s 的 CPU 时间，内存更不用说——1 万个线程 × 1MB 栈 = 10GB。

线程池的价值不在于"省了一次 clone() 的 10 微秒"，而在于**省了成千上万次创建销毁的累积开销，以及控制了并发线程总数的内存上限**。

---

## 虚拟线程来了，池化还有必要吗？

Java 21 正式引入虚拟线程（Project Loom），创建成本和 Go 的 goroutine 在一个量级：

| 维度 | 平台线程 | 虚拟线程 |
|------|---------|---------|
| 创建成本 | ~10-100μs | ~1μs |
| 内存占用 | ~512KB-1MB | ~几 KB（初始，按需增长） |
| 调度 | OS 内核调度 | JVM 调度，挂载在载体线程（ForkJoinPool）上 |
| 数量上限 | 几千（受内存限制） | 数百万 |

JDK 文档明确说：**不要池化虚拟线程**。它们就是用来创建了就用、用完就扔的。一个请求一个虚拟线程，一百万个请求一百万个虚拟线程。

但这不意味着"池化"这个概念过时了。需要区分：

### 线程本身不需要池了

虚拟线程创建成本接近零，池化线程的收益也接近零。`Executors.newVirtualThreadPerTaskExecutor()` 就是每个任务创建一个新的虚拟线程，没有复用逻辑。

### 载体线程池仍然存在

虚拟线程底层还是要挂载到平台线程上执行。这个平台线程池（默认是 `ForkJoinPool`，线程数 = CPU 核数）是 JVM 自动管理的。你不需要手动配置，但它确实存在。

### 连接池不能省

虚拟线程让你可以开百万个并发任务，但数据库只能承受几百个连接。如果百万个虚拟线程各自创建一个数据库连接——数据库直接崩了。

**虚拟线程解决的是"线程是稀缺资源"的问题，但没有解决"数据库连接是稀缺资源"的问题。**

连接池一直有意义，不只是因为“创建一次连接很贵”，还因为后面的资源根本扛不住无限并发。数据库连接、HTTP 连接这类东西都有上限。上限在那儿，池化就有意义。

### synchronized 的 pinning 问题

虚拟线程有一个已知的陷阱：当虚拟线程在 `synchronized` 块内阻塞（比如做 I/O），它无法从载体线程上卸载，会**钉住**（pin）载体线程。载体线程被钉住意味着其他虚拟线程无法被调度到这个载体线程上，等于退化成了平台线程模型。

解决方案：用 `ReentrantLock` 替代 `synchronized`。`ReentrantLock` 支持虚拟线程的挂载/卸载。

所以虚拟线程不是那种“切一下开关就迁完”的东西。老代码里一大堆 `synchronized` 块，还是得一点点替换。

---

## 池化的本质

把上面的分析串起来：

**池化 = 缓存昂贵的、可复用的资源。**

是否需要池化，取决于两个条件：

1. **资源创建成本高**（线程的 clone() + 栈内存、连接的 TCP/TLS/认证）
2. **资源有并发上限**（数据库 max_connections、文件描述符数量）

满足任一条件就值得池化。两个都满足就必须池化。

当技术演进消除了某个条件，对应的池化就变得不必要：

| 变化 | 结果 |
|------|------|
| 虚拟线程让线程创建成本趋近于零 | 线程不需要池化了 |
| 数据库连接仍然昂贵且有上限 | 连接池依然必要 |

---

## 回到选型这件事

回到开头的问题。

为什么会有这么多线程池？因为场景真不一样。延迟敏感的服务，先扩线程更合适；吞吐优先的批处理，更愿意先入队复用；分治计算又需要工作窃取。`ThreadPoolExecutor` 那套默认行为不可能把这些事一次都顾到，所以 Tomcat 去改提交顺序，ForkJoinPool 直接换了一种调度模型。

虚拟线程把“线程创建很贵”这件事压下去了，但池化这个想法没有一起消失。线程本身也许不用池了，连接、文件句柄、下游服务配额这些东西还是稀缺资源，照样得控。

我现在更愿意把这个问题想得朴素一点：先别急着问“该用哪个池”。先看你在怕什么。怕线程创建和销毁太频繁，就想办法把线程稳住。怕数据库扛不住，就老老实实控连接数。要是这些都不是瓶颈，那就别为了显得懂并发，先把池子摆上去。
