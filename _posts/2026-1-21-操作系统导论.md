---
layout: post
title: "操作系统导论"
date: 2026-1-21
categories: [系统性能, 操作系统]
tags: [操作系统, 读书笔记]
comments: true
author: huanghuoguoguo
yuque_url: "https://www.yuque.com/huanghuo-zcibe/ye6rw9/yrer1v5dfv96httc"
---

我最早学操作系统的时候，脑子里只有一些零散的名词：进程、线程、页表、文件系统。真要问这些东西为什么要这样设计，我当时答不上来。

后来做项目，才慢慢发现这门课一直在反复讲三件事：虚拟化、并发、持久性。名字不新鲜，难的是把它们和真实系统里的取舍对上号。

我现在再翻这本书，会更关心那些抽象是怎么收住边界的。接口只管暴露该暴露的东西，实现层把脏活留给自己。想把系统做简单，这种克制比多加几个能力更难。

---

## 关于设计原则的思考

### 不成熟优化 × 欠考虑泛化

书中有一句："不成熟的优化是所有坏事的根源。让整个应用的某一小部分变快，却没有提高整体性能，其实没有价值。"

每一次泛化都伴随语义膨胀、实现复杂度和概念债。锁、条件变量、信号量、无锁结构，各自只解决一个"最小但完整"的问题——保持"小而正交"的抽象，比追求"大一统"更利于系统正确与演进。

这两种毒性叠加，就是生产事故的常见根源：

|  | 概念膨胀 | 接口膨胀 | 依赖膨胀 |
|--|---------|---------|---------|
| 提前底层化 | 规则引擎编成 BPF 平台，业务改不动 | 暴露裸指针给上层，30% 代码在打补丁 | 多语言插件全拉满，CI 从 3min→45min |
| 过度并行化 | 动态自适应线程框架，上下文切换占 40% CPU | 异步 DSL 50+ 运算符，栈深 300+ 没人看懂 | 所有 IO 走 Netty，glibc 一升级全站崩 |
| 手工微优化 | 泛型特化 8 套原始类型，改一处要改 8 份 | 业务 inline 到公用库，300+ public 方法 | 热点路径上 SIMD+JNI，Docker 镜像 2GB |

最后经常会变成这样：新人三个月还在啃框架，看不懂业务；调用栈里七成是框架代码；十个下游团队一起把接口钉死；只换来那一点性能提升。

**解毒剂**："先让代码对，再让代码快，再让代码少——每一步都留下回滚按钮，并且按钮必须能被下个月的你一键按下。"

### 几个具体的反思

关于观测工具项目，我后来会先问自己一句：前面最核心的问题真的解决了吗？QPS/TPS 抖动检测、跌零检测、持久化、数学分析，这些没站稳，后面的泛化就容易变成自我感动。项目初期就算能预见后续扩展，我现在也更愿意先拿一个成本低、性能没那么好的方案试通。

关于抽象的时机：

- 接口只声明"做什么"，不暴露"怎么做"。早期用 ExecutorService 就够了，别急着把 mmap/sendfile/GPU 抛给调用者
- 具体实现可以脏、可以硬编码，但必须是私有的、可替换的
- 泛化时机 = 第三次出现相似需求，而不是第一次性能瓶颈
- "把优化留在实现层，把熟悉抽象留给调用层；让优化只污染源码，不污染接口"
- 泛化本身不是原罪，关键是封装边界和暴露粒度

我以为足够扩展就能增加它的生命周期，现在看来，维护好核心功能，其他功能在该基础上添加才是正确的。概念膨胀导致业务同学失去表达能力，所以要让其能写符合直觉的代码，将业务逻辑完全留在调用侧，而不是盲目减少代码。

---

## 第一部分 虚拟化

### 系统调用与特权级别

系统调用和过程调用看起来都像函数调用，但路子不一样。前者要通过一条受控的陷入内核路径，把 CPU 从用户态切到内核态；后者只是同一个权限级里的控制流跳转。磁盘 I/O、页表、网卡这些硬件资源，应用都得先经过这道门。

### CPU 虚拟化

- **进程**：我以前会把它想得很静态，像一段代码加一块内存。后来才对上号，进程更像一次运行中的程序：有自己的地址空间、寄存器状态、栈、打开文件，还有调度上下文
- **fork vs exec**：为什么不直接 exec 而是先 fork？因为 fork 后、exec 前可以做重定向、管道等操作
- **受限直接执行**：让进程直接在 CPU 上运行（性能），但通过特权级别限制其能力（安全）
- **调度**：FIFO → SJF → MLFQ → 比例份额 → 多处理器调度。我现在对 MLFQ 的印象，更接近“照顾交互型短任务”的那一套。把它直接往实时任务上套，我觉得不太对

### 内存虚拟化

地址空间 → 地址转换 → 分段 → 分页 → 多级页表 → swap

多级页表的思路是**做减法而不是做加法**。

---

## 第二部分 并发

线程 → 锁 → 基于锁的并发数据结构 → 条件变量 → 信号量 → 常见并发问题 → 基于事件的并发

---

## 第三部分 持久化

I/O 设备 → 磁盘驱动器 → RAID → 文件和目录 → 文件系统实现 → 局部性和快速文件系统 → 崩溃一致性（FSCK 和日志）→ 日志结构文件系统 → 数据完整性和保护

### 关于 IO 的理解框架

我被问过一次"你对 IO 有什么理解"。当时脑子里其实有东西，只是太散了，不知道先说哪一层。后来我才给自己凑出一个顺手的答法，先把范围摊开，再看面试官想往哪里深挖：

| 层次 | 关注点 |
|------|--------|
| 编程语言 | 系统调用（open/read/write），AIO/BIO/NIO |
| 应用系统 | 网络 IO（select/poll/epoll），序列化，协议栈 |
| 内核 | 匿名页、缺页（频繁缺页、透明大页）、mmap、page cache |
| 硬件 | HDD/SSD 顺序 vs 随机读写差异，总线延迟，NUMA |
| 冗余 | RAID 0/1/5/10 组合，对上层透明 |

我一般先把这几层快速过一遍。说到哪一层对方有兴趣，再往下展开。

---

## 第四部分 分布式

分布式系统 → 网络文件系统（NFS）→ Andrew 文件系统（AFS）

---

### 关于开放性面试问题的思考

怎么在听到开放性问题时快速规划回答的路径和范围？

这类问题通常都没有标准答案。我那时卡住的地方，是脑子里有不少知识点，却排不出一个顺序。现在回头看，先搭个框架还是有用。

之前被问到"实习中最有挑战性的问题"时，STAR 法则（Situation → Task → Action → Result）能帮助快速组织叙事结构。IO 问题也类似——先按层次（编程语言 → 应用系统 → 内核 → 硬件）搭出骨架，再在每一层填充自己了解的细节。这个方法可以推广到很多开放性问题：

- "你对 IO 有什么理解？" → 按抽象层次从上到下展开
- "你怎么构建一个高性能 Web 服务器？" → 按请求链路从接入到存储展开
- "如果再让你重新做这个项目，你会怎么做？" → 按时间线回顾决策点，说清当时的选择和现在的反思
- "你怎么测试这个分布式系统？" → 先列验收指标，再说测试手段

我现在比较在意的，是前半分钟先别乱。把入口摆清楚，后面才有空间聊细节，也更容易把问题引到自己熟一点的地方。

这篇记下来以后，我现在真正能记住的，是很多以前对不上的地方终于能慢慢串起来了。读书、实习、写项目，最后都会回到这些很老的主题上。只是以前我只会背定义，现在多少能说出一点自己的理解。
